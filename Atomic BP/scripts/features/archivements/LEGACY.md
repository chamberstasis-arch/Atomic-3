# LEGACY — archivements

Este directorio es contenido legado/documental. **No contiene código activo.**

El módulo activo de logros está en `features/achievements/`.

Este directorio puede ser movido fuera de `features/` o eliminado si su contenido documental
ya fue migrado a `achievements/`.
